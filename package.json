{
  "scripts": {
    "mod": "jscodeshift",
    "seed": "cp -a seeds/. examples/",
    "format": "prettier --write '**/*.{js,json,md}'",
    "format:changed": "pretty-quick",
    "precommit": "pretty-quick --staged"
  },
  "dependencies": {
    "jscodeshift": "^0.5.0"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.13.2",
    "pretty-quick": "^1.6.0"
  }
}
